import numpy as np

# generates array 1-20, step length can be given as a 3rd argument
n = np.arange(1, 21)

# generates array with from x-y in z numbers
m = np.linspace(0, 10, 50)

print(n)
print(m)
