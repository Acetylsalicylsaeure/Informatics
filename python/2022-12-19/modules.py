# import under modified name for the script
import random as rd
# import all from math a module without prefix necessary
from math import *

# specific functions can be called by module.function()
random_number = rd.randint(0, 50)
print(random_number)

fac = factorial(random_number)
print(fac)

# some variables are predefined by models
print(pi)
